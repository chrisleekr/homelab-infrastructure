**/.terraform
**/terraform.tfstate
*.tfvars
.env
node_modules/
stage2/minio-object-storage/*.crt
stage2/host-keys
tmp
.DS_Store

# Ignore root directory container
container/root
.ansible

.cursor
tmp
.claude/*

# MkDocs build output and its virtualenv. sync-versions.sh uses `sed -i.bak`.
site/
.venv-docs/
*.bak

# Taskfile checksum cache, written by the docs:install sources/generates guard
.task/
__pycache__/
